@using EventsWebsite.Models;
@model List<EventModel>
@{
    ViewBag.Title = "Event";
}

<h2>Event</h2>

@foreach (EventModel Events in Model)
{
    <a href="@Url.Action("Reserveer","Event",new { id = Events.ID})">@Events.Name></a>
}

